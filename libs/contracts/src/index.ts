/**
 * Shared Zod contracts surfaced in the OpenAPI spec.
 * These are the stable, versioned DTO shapes generated from the API spec.
 * Consumer: rally-web (via orval codegen).
 */

export * from './pagination.contract';
export * from './error.contract';
